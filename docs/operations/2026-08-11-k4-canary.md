# Pi K4 Canary — Execution Record (2026-08-11)

> Fail-closed actor gates landed on Forgejo `main` via PR #265, then this
> inspection canary ran with an auditable actor.
> Status: **executed, advisory** — production default kernel remains LOS.

## Execution chain [E]

| Step | Object | Result |
| --- | --- | --- |
| 1. source run spec | `run-k4-source-1786453393105` (audit / read-only / plan_approved) | AP2 plan + command verification `true` |
| 2. experiment | `experiment-k4-canary-1786453393105` | draft → select-candidate → approved |
| 3. candidate | `run-experiment-k4-canary-1786453393105-candidate` | `pi@0.81.1+los.3` disposition **inspection**, plan_approved, `recoveryPolicy=explicit_only` |
| 4. authorize-canary | same | `granted`, `grantedBy=operator:canary-20260811` |
| 5. execute | same | experiment **blocked** (verification gate); kernel completed |

## Kernel evidence [E]

- `run.kernel_canary_authorized` id **453861** — actor `operator:canary-20260811`
- `kernel.started` id **453869** — `pi@0.81.1+los.3`
- `kernel.finished` id **453875** — loopCount 1, textLength 621, tokens 4712/294

## Notes

1. **Planning disposition + `plan_approved` still conflict** at execute
   (`Planning disposition requires phase 'planning'`). Inspection disposition is
   the working authorize→execute path today.
2. Actor fail-closed from PR #265 held: grant is `operator:canary-20260811`, not
   `unknown`.
3. Todo `todo-los-pi-k4-readonly-canary`: `providerCanaryExecuted=true`,
   `nextAuthorization=not_granted`, `lastCanary` points at this experiment.
4. Production kernel remains **los**; no promotion.

## Delivery context

| Surface | Ref |
| --- | --- |
| Forgejo merge | PR #265 → `5fc9980545ef…` |
| GitHub mirror | PR #245 |
| Stack commits | wechat channel recovery + K4 canary actor gates |
