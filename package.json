{
  "name": "los",
  "private": true,
  "description": "Lightweight Agent Execution + Memory Management Platform",
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "check": "turbo check && ./tools/check-structure.sh",
    "test": "turbo test",
    "clean": "turbo clean"
  },
  "devDependencies": {
    "turbo": "^2.0.0",
    "typescript": "^5.5.0"
  },
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=20"
  }
}
