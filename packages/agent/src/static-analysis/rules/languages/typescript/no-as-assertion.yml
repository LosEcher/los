id: lang.typescript.no-as-assertion
language: TypeScript
severity: info
message: "Prefer type annotations over 'as' assertions"
# `as const` is a legitimate literal-type idiom, not a type-narrowing assertion.
rule:
  all:
    - kind: as_expression
    - not:
        pattern: "$A as const"
