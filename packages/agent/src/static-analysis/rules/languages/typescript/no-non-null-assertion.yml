id: lang.typescript.no-non-null-assertion
language: TypeScript
severity: warning
message: "Avoid non-null assertions (!) — use explicit null checks"
rule:
  pattern: $A!
# Test fixtures commonly use first!/second! on array access; keep the gate
# focused on production code.
exclude:
  - "\\.test\\.ts$"
